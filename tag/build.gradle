plugins {
    id 'com.android.library'
    id 'maven-publish'
}

android {
    namespace 'org.nuclearfog.tag'
    compileSdkVersion 34
    buildToolsVersion '34.0.0'
    defaultConfig {
        minSdkVersion 8
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                groupId 'com.github.nuclearfog'
                artifactId 'Tagger'
                version '2.4'
            }
        }
    }
}

dependencies {
    implementation 'androidx.annotation:annotation:1.7.0'
}